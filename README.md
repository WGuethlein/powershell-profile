In both admin/user powershell shell, type: notepad $PROFILE
It'll either open a file or ask you to create a new one, create it.
Put in: . "C:\PATH\TO\...\...\Microsoft.PowerShell_profile.ps1"
Save/Close

In powershell type: . $PROFILE
to reload shell.
